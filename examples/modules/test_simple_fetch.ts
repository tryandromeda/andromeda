// Simple test for fetch functionality
console.log("Testing if fetch is available:");
console.log("typeof fetch:", typeof fetch);

if (typeof fetch !== 'undefined') {
    console.log("fetch is available!");
}